{
  "name": "releasepilot",
  "version": "1.0.0",
  "private": true,
  "description": "DevOps capstone service health API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "lint": "node --check src/server.js && node --check test/server.test.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "4.22.2",
    "prom-client": "15.1.3"
  },
  "devDependencies": {
    "supertest": "7.0.0"
  }
}
