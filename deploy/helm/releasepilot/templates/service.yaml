apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels: { app.kubernetes.io/name: releasepilot }
spec:
  selector: { app.kubernetes.io/name: releasepilot }
  ports: [{ name: http, port: {{ .Values.service.port }}, targetPort: http }]
